#ifndef __PID_H
#define __PID_H

#include "sys.h" 

// ?? PID ???
typedef struct {
    float target_val;   // ????? (?????/??)
    float actual_val;   // ?????
    float err;          // ???? (??? - ???)
    float err_last;     // ??????
    float integral;     // ?????
    
    float Kp, Ki, Kd;   // ??(P)???(I)???(D)??
    float out_max;      // ?????? (?PWM??? 1000)
    float out_min;      // ?????? (?PWM??? 0)
} PID_TypeDef;

// ????
void PID_Init(PID_TypeDef *pid, float p, float i, float d, float max, float min);
float PID_Compute(PID_TypeDef *pid, float actual_val);

#endif